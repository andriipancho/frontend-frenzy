# Challenge title

Describe the required type or behavior, the existing problem, and objective acceptance criteria. Do not include the final implementation.

## Constraints

- Keep the public API named in `task.ts`.
- Do not weaken the supplied types or tests.
- Avoid assertions unless explicitly allowed.
