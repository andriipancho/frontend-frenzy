# TS-REC-033 — Remove every string space

Implement `RemoveSpaces` to remove every string space without losing the relationships shown by the contract.

## Constraints

- Recurse through one character that is discarded or preserved.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
