# TS-REC-044 — Freeze arrays at every depth

Implement `DeepReadonlyArrays` to freeze arrays at every depth without losing the relationships shown by the contract.

## Constraints

- Recurse through array boundaries and recursion through their elements and object properties.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
