# TS-REC-042 — Omit matching keys at every depth

Implement `DeepOmitKey` to omit matching keys at every depth without losing the relationships shown by the contract.

## Constraints

- Recurse through key removal followed by recursion into retained properties.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
