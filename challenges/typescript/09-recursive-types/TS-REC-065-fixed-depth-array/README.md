# TS-REC-065 — Build fixed-depth array types

Implement `ArrayToDepth` to build fixed-depth array types without losing the relationships shown by the contract.

## Constraints

- Recurse through a depth counter and one array layer per step.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
