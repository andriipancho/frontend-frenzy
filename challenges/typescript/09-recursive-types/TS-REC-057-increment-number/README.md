# TS-REC-057 — Increment numeric literals recursively

Implement `Increment` to increment numeric literals recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through parallel counters offset by one element.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
