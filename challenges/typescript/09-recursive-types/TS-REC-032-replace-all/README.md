# TS-REC-032 — Replace every string occurrence

Implement `ReplaceAll` to replace every string occurrence without losing the relationships shown by the contract.

## Constraints

- Recurse through the first match and recursively remaining text.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
