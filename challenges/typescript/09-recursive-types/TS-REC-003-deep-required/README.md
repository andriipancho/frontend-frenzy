# TS-REC-003 — Require nested properties

Implement `DeepRequired` to require nested properties without losing the relationships shown by the contract.

## Constraints

- Recurse through leaf boundaries and required recursive properties.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
