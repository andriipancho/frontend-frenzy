# TS-REC-024 — Remove duplicate tuple members

Implement `UniqueTuple` to remove duplicate tuple members without losing the relationships shown by the contract.

## Constraints

- Recurse through a seen-members accumulator and recursive membership.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
