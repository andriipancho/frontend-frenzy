# TS-REC-026 — Trim all leading spaces

Implement `TrimStart` to trim all leading spaces without losing the relationships shown by the contract.

## Constraints

- Recurse through one leading space per recursion step.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
