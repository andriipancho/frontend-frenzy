# TS-REC-028 — Trim spaces from both ends

Implement `TrimText` to trim spaces from both ends without losing the relationships shown by the contract.

## Constraints

- Recurse through leading and trailing space cases before the stable base case.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
