# TS-REC-052 — Collect keys from every object depth

Implement `NestedKeys` to collect keys from every object depth without losing the relationships shown by the contract.

## Constraints

- Recurse through current keys unioned with recursively nested keys.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
