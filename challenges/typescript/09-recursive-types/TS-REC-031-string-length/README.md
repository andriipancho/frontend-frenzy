# TS-REC-031 — Count string characters

Implement `StringLength` to count string characters without losing the relationships shown by the contract.

## Constraints

- Recurse through one UTF-16 code unit and a counter accumulator.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
