# TS-REC-034 — Convert snake case recursively

Implement `SnakeToCamel` to convert snake case recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through one underscore-delimited segment and recursively converted remainder.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
