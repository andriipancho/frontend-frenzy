# TS-REC-037 — Parse every path segment

Implement `PathSegments` to parse every path segment without losing the relationships shown by the contract.

## Constraints

- Recurse through one slash-delimited path segment and remaining path.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
