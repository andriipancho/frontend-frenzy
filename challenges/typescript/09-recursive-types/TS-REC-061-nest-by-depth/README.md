# TS-REC-061 — Nest values to a fixed depth

Implement `NestValue` to nest values to a fixed depth without losing the relationships shown by the contract.

## Constraints

- Recurse through a depth accumulator and one wrapper per step.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
