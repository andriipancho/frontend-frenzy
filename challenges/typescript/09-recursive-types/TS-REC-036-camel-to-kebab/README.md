# TS-REC-036 — Convert camel case recursively

Implement `CamelToKebab` to convert camel case recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through one character classified by case before recursing.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
