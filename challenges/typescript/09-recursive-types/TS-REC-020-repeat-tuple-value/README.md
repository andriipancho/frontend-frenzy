# TS-REC-020 — Repeat values into tuples

Implement `RepeatValue` to repeat values into tuples without losing the relationships shown by the contract.

## Constraints

- Recurse through an accumulator whose length reaches the requested count.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
