# TS-REC-027 — Trim all trailing spaces

Implement `TrimEnd` to trim all trailing spaces without losing the relationships shown by the contract.

## Constraints

- Recurse through one trailing space per recursion step.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
