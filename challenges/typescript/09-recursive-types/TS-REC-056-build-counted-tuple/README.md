# TS-REC-056 — Build tuples to a numeric length

Implement `BuildTuple` to build tuples to a numeric length without losing the relationships shown by the contract.

## Constraints

- Recurse through a tuple accumulator whose length reaches the target.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
