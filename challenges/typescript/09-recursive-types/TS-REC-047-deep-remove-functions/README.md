# TS-REC-047 — Remove function properties recursively

Implement `DeepDataOnly` to remove function properties recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through function-valued key filtering before recursion into data properties.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
