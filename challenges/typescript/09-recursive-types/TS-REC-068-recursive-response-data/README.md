# TS-REC-068 — Extract nested response data

Implement `ResponseData` to extract nested response data without losing the relationships shown by the contract.

## Constraints

- Recurse through data envelopes before recursive tuple traversal.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
