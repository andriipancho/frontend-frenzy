# TS-REC-060 — Measure array nesting depth

Implement `ArrayDepth` to measure array nesting depth without losing the relationships shown by the contract.

## Constraints

- Recurse through one array layer and a depth accumulator.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
