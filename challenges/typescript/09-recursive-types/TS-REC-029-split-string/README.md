# TS-REC-029 — Split strings recursively

Implement `SplitText` to split strings recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through the first separated segment and recursively remaining text.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
