# TS-REC-035 — Convert kebab case recursively

Implement `KebabToCamel` to convert kebab case recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through one hyphen-delimited segment and recursively converted remainder.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
