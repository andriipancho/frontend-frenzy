# TS-REC-014 — Reverse tuples recursively

Implement `ReverseTuple` to reverse tuples recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through head extraction followed by recursive tail reversal.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
