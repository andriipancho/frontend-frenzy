# TS-REC-051 — Unwrap arrays throughout objects

Implement `DeepArrayItems` to unwrap arrays throughout objects without losing the relationships shown by the contract.

## Constraints

- Recurse through array element recursion before object property recursion.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
