# TS-REC-008 — Flatten nested array elements

Implement `DeepElement` to flatten nested array elements without losing the relationships shown by the contract.

## Constraints

- Recurse through array element layers until a non-array base case.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
