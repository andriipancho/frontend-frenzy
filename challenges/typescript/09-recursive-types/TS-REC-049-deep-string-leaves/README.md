# TS-REC-049 — Convert nested leaves to strings

Implement `DeepStrings` to convert nested leaves to strings without losing the relationships shown by the contract.

## Constraints

- Recurse through leaf replacement beneath recursive tuples and objects.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
