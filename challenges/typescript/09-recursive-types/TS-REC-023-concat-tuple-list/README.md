# TS-REC-023 — Concatenate a tuple list

Implement `ConcatTupleList` to concatenate a tuple list without losing the relationships shown by the contract.

## Constraints

- Recurse through one tuple segment followed by recursively concatenated segments.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
