# TS-REC-039 — Generate nested object paths

Implement `ObjectPaths` to generate nested object paths without losing the relationships shown by the contract.

## Constraints

- Recurse through each key plus recursively prefixed child paths.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
