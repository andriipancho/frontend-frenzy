# TS-REC-010 — Replace nested leaves with boolean

Implement `DeepBooleans` to replace nested leaves with boolean without losing the relationships shown by the contract.

## Constraints

- Recurse through function and primitive leaves under recursive tuple and object mapping.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
