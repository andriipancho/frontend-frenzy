# TS-REC-059 — Compare tuple lengths recursively

Implement `SameLength` to compare tuple lengths recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through one element removed from both tuples per step.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
