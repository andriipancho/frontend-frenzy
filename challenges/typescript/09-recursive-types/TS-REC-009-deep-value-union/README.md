# TS-REC-009 — Collect nested object values

Implement `DeepValues` to collect nested object values without losing the relationships shown by the contract.

## Constraints

- Recurse through object property unions and primitive leaf cases.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
