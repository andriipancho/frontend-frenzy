# TS-REC-015 — Flatten nested tuples recursively

Implement `FlattenTuple` to flatten nested tuples recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through nested tuple heads and remaining tuple tails.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
