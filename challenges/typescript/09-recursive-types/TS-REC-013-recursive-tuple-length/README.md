# TS-REC-013 — Count tuple elements recursively

Implement `TupleLength` to count tuple elements recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through one tuple element per recursion step.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
