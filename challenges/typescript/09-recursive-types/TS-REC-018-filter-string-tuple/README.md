# TS-REC-018 — Keep string tuple members

Implement `StringMembers` to keep string tuple members without losing the relationships shown by the contract.

## Constraints

- Recurse through string heads that are retained before recursive filtering.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
