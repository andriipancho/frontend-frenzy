# TS-REC-016 — Search tuple members recursively

Implement `TupleIncludes` to search tuple members recursively without losing the relationships shown by the contract.

## Constraints

- Recurse through exact head comparison before searching the tuple tail.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
