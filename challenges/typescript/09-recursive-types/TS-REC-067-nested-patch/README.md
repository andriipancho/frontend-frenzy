# TS-REC-067 — Design nested patch contracts

Implement `DeepPatch` to design nested patch contracts without losing the relationships shown by the contract.

## Constraints

- Recurse through array replacement boundaries and optional recursive object patches.
- Define a terminating base case before or alongside the recursive branch.
- Do not use any.
