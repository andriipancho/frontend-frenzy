# TS-CORE-018 — Model a possibly missing first item

Make `firstItem` accurately represent that an array lookup may not find an element.

## Constraints

- Keep the input as a string array.
- Do not add a fallback value.
- Reflect absence in the return type.
