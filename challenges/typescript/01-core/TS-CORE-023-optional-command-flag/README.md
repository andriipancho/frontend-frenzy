# TS-CORE-023 — Add an optional tuple flag

Model `Command` as a command name followed by an optional force flag.

## Constraints

- Require the command name.
- Allow omission of the boolean flag.
- Reject additional tuple positions.
