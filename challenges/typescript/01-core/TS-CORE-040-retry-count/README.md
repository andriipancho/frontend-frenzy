# TS-CORE-040 — Bound retry count literals

Constrain `RetryCount` to the supported values from zero through three.

## Constraints

- Use numeric literal types.
- Allow exactly four values.
- Reject negative and larger counts.
