# TS-CORE-022 — Define a coordinate tuple

Model `Point` as exactly two numeric coordinates rather than an arbitrary-length numeric array.

## Constraints

- Require exactly two elements.
- Keep both elements numeric.
- Reject missing or extra coordinates.
