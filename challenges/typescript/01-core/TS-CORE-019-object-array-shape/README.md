# TS-CORE-019 — Correct an object array shape

Correct the metric seed data so every array element satisfies the declared `Metric` object type.

## Constraints

- Keep `Metric` unchanged.
- Keep the same metric name and numeric meaning.
- Do not use an assertion.
