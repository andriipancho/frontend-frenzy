# TS-CORE-004 — Annotate a numeric total

Annotate `calculateTotal` so strict TypeScript understands the input collection and infers a numeric result.

## Constraints

- Keep the existing calculation.
- Do not use `any` or an assertion.
- Reject arrays containing non-number values.
