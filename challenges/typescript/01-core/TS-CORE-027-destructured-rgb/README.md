# TS-CORE-027 — Type a destructured RGB tuple

Constrain the destructured `rgb` parameter to exactly three numeric channels so each local value is known to exist.

## Constraints

- Keep parameter destructuring.
- Require three numeric channels.
- Return the existing CSS string.
