# TS-CORE-035 — Type a numeric dictionary

Define `Counters` as an object that accepts arbitrary string keys whose values are numbers.

## Constraints

- Do not enumerate specific counter names.
- Reject non-number values.
- Keep property access string-based.
