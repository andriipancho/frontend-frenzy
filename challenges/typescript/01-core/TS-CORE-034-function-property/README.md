# TS-CORE-034 — Type a formatter property

Correct `NumberFormatter` so `format` is a function that converts a number to text.

## Constraints

- Keep the property name.
- Accept one number argument.
- Return a string.
