# TS-CORE-015 — Honor a numeric return contract

Correct `parsePort` so its type and implementation return a numeric port from textual configuration.

## Constraints

- Keep the string input.
- Return a number.
- Do not use an assertion.
