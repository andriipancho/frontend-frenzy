# TS-CORE-020 — Model a numeric matrix

Correct `Matrix` so it represents rows of numbers rather than a single flat row.

## Constraints

- Keep the sample matrix nested.
- Allow any number of rows and columns.
- Do not use tuples for variable-length rows.
