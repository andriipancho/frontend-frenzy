# TS-CORE-016 — Type a numeric score list

Correct `scores` so the array annotation matches the numeric values it stores.

## Constraints

- Keep all three score values.
- Allow additional numbers.
- Reject string elements.
