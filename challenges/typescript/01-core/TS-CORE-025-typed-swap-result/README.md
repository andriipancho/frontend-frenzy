# TS-CORE-025 — Return a precise tuple

Give `swapEntry` a tuple return type that preserves the fixed string-then-number positions.

## Constraints

- Keep the runtime element order.
- Return exactly two elements.
- Do not use an assertion.
