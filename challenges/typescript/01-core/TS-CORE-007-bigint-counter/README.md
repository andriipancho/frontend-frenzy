# TS-CORE-007 — Use a bigint counter

Model `Counter` and its initial value with the primitive intended for integers beyond the safe number range.

## Constraints

- Keep the exported type and value names.
- Do not mix number and bigint values.
- Initialize the counter at zero.
