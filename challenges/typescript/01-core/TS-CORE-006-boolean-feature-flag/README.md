# TS-CORE-006 — Return a boolean feature flag

Make `isFeatureEnabled` preserve the boolean value it receives instead of converting it to text.

## Constraints

- Keep the parameter boolean.
- Return a boolean.
- Do not use an assertion.
