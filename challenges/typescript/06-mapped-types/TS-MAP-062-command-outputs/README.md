# TS-MAP-062 — Map command outputs

Create a command output map from a union of command contracts.

## Constraints

- Use command names as keys.
- Store exact output types.
- Preserve synchronous shape information.
