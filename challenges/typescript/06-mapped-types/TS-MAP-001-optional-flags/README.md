# TS-MAP-001 — Map optional flags

Implement `OptionalFlags<T>` so every key from `T` becomes an optional boolean flag, regardless of its original value type.

## Constraints

- Preserve exactly the original keys.
- All values must be boolean when present.
- Do not use built-in utility types.
