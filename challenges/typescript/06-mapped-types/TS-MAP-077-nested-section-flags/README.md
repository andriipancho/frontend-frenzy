# TS-MAP-077 — Map nested section flags

Replace every nested configuration value with a required boolean flag.

## Constraints

- Preserve both key levels.
- Use boolean at every leaf.
- Keep all sections and fields required.
