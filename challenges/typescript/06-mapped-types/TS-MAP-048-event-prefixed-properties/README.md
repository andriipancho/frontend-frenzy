# TS-MAP-048 — Keep event-prefixed properties

Filter an object to keys beginning with on followed by an event name.

## Constraints

- Keep matching keys unchanged.
- Remove every non-handler key.
- Preserve handler signatures.
