# TS-MAP-021 — Make selected properties optional

Make only the chosen keys optional while leaving every other property required.

## Constraints

- Restrict K to source keys.
- Preserve unselected properties.
- Do not use Partial, Pick, or Omit.
