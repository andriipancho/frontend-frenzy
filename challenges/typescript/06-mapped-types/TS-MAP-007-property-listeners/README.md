# TS-MAP-007 — Map property listeners

Create one listener function per property using that property's exact value type.

## Constraints

- Preserve property names.
- Each listener accepts its related value type.
- Every listener returns void.
