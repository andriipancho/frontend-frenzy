# TS-MAP-060 — Map responses by route

Derive a response lookup from a union of route contracts.

## Constraints

- Use route as the key.
- Select response as the value.
- Preserve exact route-response pairs.
