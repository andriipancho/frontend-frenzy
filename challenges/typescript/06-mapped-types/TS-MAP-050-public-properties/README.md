# TS-MAP-050 — Remove internal properties

Filter out string keys beginning with an underscore.

## Constraints

- Remove underscore-prefixed keys.
- Keep all public keys unchanged.
- Preserve public value types.
