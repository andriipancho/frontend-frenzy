# TS-MAP-082 — Map property patch operations

Create an optional per-property operation containing a typed set value or clear flag.

## Constraints

- Preserve source keys.
- Allow operations to be omitted.
- Relate set values to their properties.
