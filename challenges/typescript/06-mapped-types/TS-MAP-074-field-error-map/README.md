# TS-MAP-074 — Map optional field errors

Create an optional string error for every form field.

## Constraints

- Preserve exactly form keys.
- Allow errors to be omitted.
- Use string error messages.
