# TS-MAP-022 — Require selected properties

Require chosen optional keys while preserving optionality on all other fields.

## Constraints

- Restrict K to source keys.
- Remove optionality only from selected keys.
- Do not use Required, Pick, or Omit.
