# TS-MAP-009 — Map keys to their names

Create an object type where each property's value is its own key literal.

## Constraints

- Preserve the source key set.
- Use the current mapped key as its value.
- Do not widen values to PropertyKey.
