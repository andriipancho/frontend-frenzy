# TS-MAP-087 — Generate API mock handlers

Generate synchronous-or-async mock handlers from route contracts.

## Constraints

- Preserve route keys.
- Accept exact request types.
- Return responses directly or through Promise.
