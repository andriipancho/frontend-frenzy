# TS-MAP-072 — Map nested optional sections

Keep top-level sections required while making every field inside each section optional.

## Constraints

- Preserve top-level section keys.
- Optionalize nested fields only.
- Do not use Partial.
