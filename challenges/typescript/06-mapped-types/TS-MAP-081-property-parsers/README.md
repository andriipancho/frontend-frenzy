# TS-MAP-081 — Map property parsers

Create a string parser returning the related type for every property.

## Constraints

- Preserve source keys.
- Accept string input for all parsers.
- Return each exact property type.
