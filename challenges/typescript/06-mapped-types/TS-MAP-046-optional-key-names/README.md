# TS-MAP-046 — Collect optional key names

Produce a union containing only optional property names.

## Constraints

- Return key names rather than an object.
- Preserve string, number, or symbol keys.
- Detect declared optionality.
