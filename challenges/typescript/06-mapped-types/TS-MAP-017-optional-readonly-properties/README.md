# TS-MAP-017 — Map optional readonly properties

Make every property both optional and readonly in one mapped transformation.

## Constraints

- Preserve original value types.
- Add both property modifiers.
- Do not compose built-in utility types.
