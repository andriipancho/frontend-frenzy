# TS-MAP-089 — Map feature resolvers

Create synchronous-or-async resolver functions for every feature configuration.

## Constraints

- Preserve feature keys.
- Return related configuration values.
- Allow Promise results.
