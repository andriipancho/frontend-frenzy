# TS-MAP-041 — Keep function properties

Filter an object type down to properties whose values are callable.

## Constraints

- Preserve original function signatures.
- Remove non-function properties.
- Preserve matching keys.
