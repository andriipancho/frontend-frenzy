# TS-MAP-067 — Map named getters

Create getter methods from a union of names sharing one value type.

## Constraints

- Prefix each name with get.
- Capitalize names.
- Return the supplied value type.
