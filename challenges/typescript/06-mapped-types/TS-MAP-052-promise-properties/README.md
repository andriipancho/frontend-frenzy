# TS-MAP-052 — Keep Promise properties

Filter an object to properties whose values are Promises.

## Constraints

- Keep Promise-valued keys.
- Remove synchronous values.
- Preserve exact Promise result types.
