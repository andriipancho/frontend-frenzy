# TS-MAP-097 — Generate state selectors

Generate select-prefixed functions for every state slice.

## Constraints

- Prefix and capitalize slice keys.
- Accept the complete state.
- Return the selected slice type.
