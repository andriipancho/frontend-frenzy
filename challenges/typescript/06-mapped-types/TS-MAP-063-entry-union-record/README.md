# TS-MAP-063 — Map entry tuples to a record

Convert a union of readonly key-value tuples into an object type.

## Constraints

- Use tuple position zero as the key.
- Use tuple position one as the value.
- Support every PropertyKey kind.
