# TS-MAP-080 — Map property serializers

Create a string serializer for every property value.

## Constraints

- Preserve source keys.
- Accept each related value type.
- Return string from every serializer.
