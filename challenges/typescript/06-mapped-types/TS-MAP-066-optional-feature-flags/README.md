# TS-MAP-066 — Map optional feature flags

Create optional boolean properties from a feature-name union.

## Constraints

- Allow any feature to be omitted.
- Reject unrelated keys.
- Use boolean when a flag is present.
