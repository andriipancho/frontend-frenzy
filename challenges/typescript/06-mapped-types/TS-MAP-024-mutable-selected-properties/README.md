# TS-MAP-024 — Unlock selected properties

Remove readonly from selected keys while preserving readonly on the others.

## Constraints

- Restrict K to source keys.
- Make only selected keys mutable.
- Do not use built-in property utilities.
