# TS-MAP-008 — Map property validators

Create a boolean validator for every property using its associated value type.

## Constraints

- Preserve every source key.
- Relate validator inputs to their properties.
- Return boolean from all validators.
