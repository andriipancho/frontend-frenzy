# TS-MAP-091 — Map form field codecs

Create a parse and format codec for every form field.

## Constraints

- Preserve field keys.
- Parse strings to related values.
- Format related values to strings.
