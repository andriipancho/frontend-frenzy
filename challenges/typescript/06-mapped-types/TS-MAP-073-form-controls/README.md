# TS-MAP-073 — Map form controls

Create a control state for every field using its exact field value type.

## Constraints

- Preserve form field keys.
- Keep related values.
- Add touched and disabled flags.
