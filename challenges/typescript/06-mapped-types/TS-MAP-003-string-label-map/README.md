# TS-MAP-003 — Map properties to labels

Transform every property value into a string label while retaining the original keys.

## Constraints

- Preserve source keys.
- Use string for every output value.
- Do not use built-in utility types.
