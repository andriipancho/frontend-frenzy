# TS-MAP-042 — Keep string properties

Filter an object type to properties whose values are strings.

## Constraints

- Keep string-valued keys.
- Remove every other value category.
- Preserve string value types.
