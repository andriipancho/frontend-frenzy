# TS-MAP-040 — Namespace property keys

Prefix every string key with a namespace separated by a dot.

## Constraints

- Use the namespace type parameter.
- Preserve source key spelling.
- Preserve values.
