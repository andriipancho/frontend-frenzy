# TS-MAP-010 — Map tagged property values

Wrap every property in an object containing its key and original value type.

## Constraints

- Preserve each source key.
- Keep the key literal inside each wrapper.
- Keep the related original value type.
