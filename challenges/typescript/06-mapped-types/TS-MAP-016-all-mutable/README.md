# TS-MAP-016 — Map all properties mutable

Remove readonly from every source property.

## Constraints

- Preserve keys, values, and optionality.
- Remove readonly modifiers explicitly.
- Do not use a built-in mutable helper.
