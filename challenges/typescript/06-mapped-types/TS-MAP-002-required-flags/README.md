# TS-MAP-002 — Map required flags

Transform every source property into a required boolean flag.

## Constraints

- Preserve exactly the source keys.
- Replace every value type with boolean.
- Do not use Record or another built-in utility.
