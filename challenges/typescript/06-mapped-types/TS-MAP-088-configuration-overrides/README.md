# TS-MAP-088 — Map configuration overrides

Allow optional sections containing optional nested configuration overrides.

## Constraints

- Allow whole sections to be omitted.
- Allow individual nested fields to be omitted.
- Preserve every configured value type.
