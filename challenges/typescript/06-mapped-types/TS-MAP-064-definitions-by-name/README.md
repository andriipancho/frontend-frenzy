# TS-MAP-064 — Index definitions by name

Turn a union of named definitions into a name-keyed lookup.

## Constraints

- Use name as each key.
- Preserve full definition objects.
- Require PropertyKey-compatible names.
