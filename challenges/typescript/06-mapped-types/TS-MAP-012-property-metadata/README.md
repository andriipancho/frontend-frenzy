# TS-MAP-012 — Map property metadata

Create metadata entries that retain each original property value type.

## Constraints

- Preserve all source keys.
- Keep the related value type.
- Add shared dirty and touched flags.
