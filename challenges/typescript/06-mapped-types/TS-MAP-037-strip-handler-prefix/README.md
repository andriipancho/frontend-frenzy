# TS-MAP-037 — Strip handler prefixes

Convert on-prefixed handler names back to uncapitalized event names.

## Constraints

- Transform on-prefixed keys.
- Drop keys without the prefix.
- Preserve handler value types.
