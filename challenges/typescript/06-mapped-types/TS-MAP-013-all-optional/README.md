# TS-MAP-013 — Map all properties optional

Make every property optional with a mapped property modifier.

## Constraints

- Preserve keys and values.
- Add optionality to all properties.
- Do not use Partial.
