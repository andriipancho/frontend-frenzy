# TS-MAP-014 — Map all properties required

Remove optionality from every source property.

## Constraints

- Preserve keys and value types.
- Remove optional modifiers explicitly.
- Do not use Required.
