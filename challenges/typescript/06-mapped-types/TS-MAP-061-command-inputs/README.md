# TS-MAP-061 — Map command inputs

Create a command input map from a union of command contracts.

## Constraints

- Use command names as keys.
- Store only each input type.
- Preserve exact relationships.
