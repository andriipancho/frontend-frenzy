# TS-MAP-005 — Map nullable properties

Allow null for each property while preserving its original value type.

## Constraints

- Keep the same keys.
- Preserve each original value type.
- Add null independently to every property.
