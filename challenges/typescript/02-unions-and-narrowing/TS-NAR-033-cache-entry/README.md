# TS-NAR-033 — Narrow a cache entry

Describe cache hits and misses using their distinct object properties.

## Constraints

- Keep hit and miss shapes unchanged.
- Return a string for both branches.
- Do not add a discriminant property.
