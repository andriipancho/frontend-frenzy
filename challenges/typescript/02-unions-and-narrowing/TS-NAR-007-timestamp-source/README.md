# TS-NAR-007 — Accept date or epoch timestamps

Allow a timestamp to arrive as a Date instance or epoch milliseconds.

## Constraints

- Keep the exported names.
- Do not use `any` or assertions.
- Preserve the accepted cases demonstrated by the tests.
