# TS-NAR-045 — Narrow an optional item list

Read an item count from an array that may not have been loaded.

## Constraints

- Accept a string array or undefined.
- Return zero for absent input.
- Do not create a replacement array.
