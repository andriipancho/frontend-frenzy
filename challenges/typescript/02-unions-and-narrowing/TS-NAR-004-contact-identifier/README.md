# TS-NAR-004 — Model contact identifiers

Represent contact identifiers accepted from both modern string IDs and legacy numeric IDs.

## Constraints

- Keep the exported names.
- Do not use `any` or assertions.
- Preserve the accepted cases demonstrated by the tests.
