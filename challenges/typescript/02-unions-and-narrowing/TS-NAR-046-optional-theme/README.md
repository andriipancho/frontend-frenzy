# TS-NAR-046 — Narrow optional nested settings

Read a nested theme name only when the optional theme object exists.

## Constraints

- Keep the optional property.
- Use a presence branch.
- Return a string fallback.
