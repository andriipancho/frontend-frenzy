# TS-NAR-016 — Combine payload and version metadata

Require a payload body together with immutable version metadata.

## Constraints

- Keep the exported names.
- Do not use `any` or assertions.
- Preserve the accepted cases demonstrated by the tests.
