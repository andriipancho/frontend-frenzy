# TS-NAR-073 — Narrow mixed selection input

Normalize a selection supplied as an ID string, an identified object, or null.

## Constraints

- Keep all three input members.
- Return an ID or undefined.
- Use safe runtime narrowing without assertions.
