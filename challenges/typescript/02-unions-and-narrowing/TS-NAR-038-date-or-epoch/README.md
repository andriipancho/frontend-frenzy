# TS-NAR-038 — Narrow Date or epoch

Convert either a Date instance or an existing epoch number into epoch milliseconds.

## Constraints

- Keep both accepted input forms.
- Use a class-instance check.
- Return a number in every branch.
