# TS-NAR-047 — Combine truthiness with unknown

Normalize an unknown value only when it is a non-empty string.

## Constraints

- Keep the input unknown.
- Combine safe runtime checks.
- Return undefined for every other value.
