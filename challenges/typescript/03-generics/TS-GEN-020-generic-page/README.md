# TS-GEN-020 — Parameterize paginated items

Make `Page<T>` expose an array of the requested item type.

## Constraints

- Keep pagination fields numeric.
- Use T for every item.
- Keep items mutable.
