# TS-GEN-017 — Parameterize a box type

Make `Box<T>` store the type supplied through its parameter.

## Constraints

- Keep the value property.
- Use the existing type parameter.
- Do not use unknown or any for storage.
