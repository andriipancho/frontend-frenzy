# TS-GEN-024 — Parameterize an event envelope

Make `Envelope<T>` preserve the payload model selected by its caller.

## Constraints

- Keep topic as string.
- Use T for payload.
- Do not add optional payload states.
