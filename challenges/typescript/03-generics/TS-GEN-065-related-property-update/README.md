# TS-GEN-065 — Relate update keys and values

Repair `updateProperty` so each property key accepts only its matching value type.

## Constraints

- Preserve the object, key, and value relationship.
- Reject values belonging to other properties.
- Do not use assertions.
