# TS-GEN-076 — Type a related key-value callback

Define a callback where the value type follows its fixed object key.

## Constraints

- Keep the key argument as K.
- Derive the value from T and K.
- Return void.
