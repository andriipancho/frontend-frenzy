# TS-GEN-047 — Create a heterogeneous pair

Preserve independent types for the two positions returned by `makePair`.

## Constraints

- Allow different input types.
- Preserve each tuple position.
- Do not combine values into one union parameter.
