# TS-GEN-027 — Parameterize a container interface

Make `Container<T>` return and accept the type selected by its consumer.

## Constraints

- Keep get and set methods.
- Use T in both directions.
- Do not use unknown or any.
