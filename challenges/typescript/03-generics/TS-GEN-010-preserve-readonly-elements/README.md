# TS-GEN-010 — Preserve readonly array elements

Make `copyReadonly` preserve an input array's element type in a readonly result.

## Constraints

- Accept readonly inputs.
- Return a readonly array.
- Preserve literal element members.
