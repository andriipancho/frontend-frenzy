# TS-GEN-015 — Preserve an optional generic value

Make `optionalValue` preserve its input type when conditionally returning undefined.

## Constraints

- Keep the boolean condition.
- Return undefined when excluded.
- Preserve literal input types.
