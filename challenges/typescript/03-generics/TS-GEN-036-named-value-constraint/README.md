# TS-GEN-036 — Constrain a named value type

Require `NamedValue<T>` inputs to contain a string name.

## Constraints

- Keep the alias result as T.
- Reject types without name.
- Require name to be string.
