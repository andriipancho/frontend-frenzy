# TS-GEN-062 — Default dictionary values

Make dictionary values default to strings while supporting other value types.

## Constraints

- Retain the string index signature.
- Use string as the default value type.
- Preserve explicit value types.
