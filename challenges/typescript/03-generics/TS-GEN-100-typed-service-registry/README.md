# TS-GEN-100 — Design a typed service registry

Repair a service registry contract so registrations and lookups preserve each service key's type.

## Constraints

- Only declared service keys may be used.
- Set must accept the service matching its key.
- Get must return the service matching its key.
