# TS-GEN-071 — Create a related patch entry

Tie each patch entry value to the property named by its key.

## Constraints

- K must remain constrained to object keys.
- The value must match only K.
- Do not use a union of all object values.
