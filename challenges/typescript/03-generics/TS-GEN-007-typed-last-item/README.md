# TS-GEN-007 — Preserve the last item type

Make `lastItem` accept readonly arrays and return their element type or undefined.

## Constraints

- Accept mutable and readonly arrays.
- Preserve the element type.
- Represent empty input safely.
