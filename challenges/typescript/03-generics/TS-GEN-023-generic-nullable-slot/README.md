# TS-GEN-023 — Parameterize a nullable slot

Make `NullableSlot<T>` contain the requested value type or null.

## Constraints

- Keep null as the empty marker.
- Use T for present values.
- Keep the object wrapper.
