# TS-GEN-096 — Preserve command payloads

Make a command dispatcher reject payloads intended for other commands.

## Constraints

- Accept only declared command names.
- Connect each command to its payload.
- Return void from dispatch.
