# TS-GEN-046 — Preserve endpoint configuration

Require an endpoint while preserving all environment-specific configuration fields.

## Constraints

- Require endpoint as string.
- Return the complete input model.
- Reject configs without endpoint.
