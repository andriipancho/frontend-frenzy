# TS-GEN-022 — Parameterize dictionary values

Make every value in `Dictionary<T>` use its supplied type.

## Constraints

- Keep arbitrary string keys.
- Use T for each value.
- Do not enumerate keys.
