# TS-GEN-070 — Preserve a specific sort key

Keep the selected sort key as a specific type instead of widening it to every object key.

## Constraints

- The key must belong to T.
- Preserve K in the descriptor.
- Keep direction unchanged.
