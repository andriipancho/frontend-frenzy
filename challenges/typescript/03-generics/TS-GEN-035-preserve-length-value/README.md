# TS-GEN-035 — Preserve a length-bearing value

Add `size` to any length-bearing value without losing its specific fields or methods.

## Constraints

- Require a numeric length.
- Preserve the complete input type.
- Return the added size field.
