# TS-GEN-078 — Filter generic values

Preserve an array's element type through `filterValues`.

## Constraints

- Accept a predicate over the inferred element type.
- Return the same element type.
- Support readonly input arrays.
