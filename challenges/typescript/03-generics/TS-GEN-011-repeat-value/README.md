# TS-GEN-011 — Repeat a typed value

Make `repeat` return an array whose element type matches its input.

## Constraints

- Keep the count argument numeric.
- Preserve the value type.
- Do not use assertions.
