# TS-GEN-021 — Parameterize a tree node

Propagate `T` through a tree node's value and child nodes.

## Constraints

- Keep the recursive structure.
- Use the same type at every level.
- Do not widen child values.
