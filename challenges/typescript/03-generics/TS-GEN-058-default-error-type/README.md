# TS-GEN-058 — Default result error type

Model a result whose error type defaults to `Error` but can be overridden.

## Constraints

- Preserve the data type in the success branch.
- Default the error branch to Error.
- Keep the discriminated union.
