# TS-GEN-079 — Reduce with a generic accumulator

Preserve independent element and accumulator types through `reduceValues`.

## Constraints

- Accept readonly input arrays.
- Type each callback argument precisely.
- Return the accumulator type.
