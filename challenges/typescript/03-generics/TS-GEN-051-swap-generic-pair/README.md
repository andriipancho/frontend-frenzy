# TS-GEN-051 — Swap independent pair types

Make `swapPair` reverse both values and their independent tuple types.

## Constraints

- Accept two different types.
- Return reversed positions.
- Preserve literal values.
