# TS-GEN-072 — Relate event handlers to payloads

Define an event handler whose argument is derived from the selected event name.

## Constraints

- Only known event names are valid.
- The callback must receive the matching payload.
- Keep the handler return type void.
