# TS-GEN-099 — Preserve repository query values

Type a repository helper so the query value matches the selected entity property.

## Constraints

- Accept readonly entity arrays.
- Require a valid entity key and matching value.
- Return the entity type or undefined.
