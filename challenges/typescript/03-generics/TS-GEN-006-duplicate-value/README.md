# TS-GEN-006 — Duplicate a typed value

Preserve a value type across both positions of the tuple returned by `duplicate`.

## Constraints

- Return exactly two positions.
- Keep both positions related to the input.
- Avoid overloads and assertions.
