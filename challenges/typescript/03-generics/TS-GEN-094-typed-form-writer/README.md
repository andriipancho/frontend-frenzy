# TS-GEN-094 — Preserve form field assignments

Strengthen a form writer so field names and submitted values cannot drift apart.

## Constraints

- Support every key in the form model.
- Require the selected field's value type.
- Keep the contract implementation-agnostic.
