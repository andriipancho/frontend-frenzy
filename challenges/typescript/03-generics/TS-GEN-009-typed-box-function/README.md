# TS-GEN-009 — Create a typed box

Preserve the input type in the `value` property returned by `makeBox`.

## Constraints

- Keep the returned object shape.
- Preserve caller-specific types.
- Do not use assertions.
