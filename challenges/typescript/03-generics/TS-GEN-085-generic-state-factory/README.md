# TS-GEN-085 — Create a generic state factory

Build a small state container that preserves its initial value type across reads and writes.

## Constraints

- Infer state from the initial value.
- Return typed get and set methods.
- Reject writes of unrelated types.
