# TS-GEN-061 — Default empty selection

Make an unconfigured selection state represent only an empty selection.

## Constraints

- Use the existing never default.
- An explicit item type must be nullable.
- Do not change the selected property name.
