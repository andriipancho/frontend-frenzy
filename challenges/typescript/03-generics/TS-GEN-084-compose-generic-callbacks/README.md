# TS-GEN-084 — Compose generic callbacks

Compose two callbacks while preserving the intermediate type relationship.

## Constraints

- The first result must match the second input.
- Infer the initial, intermediate, and final types.
- Return a correctly typed composed callback.
