# TS-GEN-064 — Default handler signature

Define a handler with independently defaulted input and output types.

## Constraints

- Input must default to unknown.
- Output must default to void.
- Explicit input and output types must be preserved.
