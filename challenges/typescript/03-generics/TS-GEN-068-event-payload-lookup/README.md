# TS-GEN-068 — Look up event payloads

Make `EventPayload` select the payload associated with one event name.

## Constraints

- The event name must be a valid key.
- Preserve the exact payload type.
- Do not create a manual union.
