# TS-GEN-018 — Parameterize a homogeneous pair

Make both positions of `Pair<T>` use the supplied element type.

## Constraints

- Keep exactly two positions.
- Use one type parameter.
- Preserve tuple ordering.
