# TS-GEN-074 — Compare a related property

Type `hasPropertyValue` so the comparison value matches the selected object property.

## Constraints

- Infer the selected key independently.
- Reject values for other properties.
- Return a boolean without assertions.
