# TS-GEN-077 — Map with a generic callback

Make `mapValues` infer both its element and callback result types.

## Constraints

- Accept readonly arrays.
- Contextually type the callback input.
- Return an array of callback results.
