# TS-GEN-005 — Wrap a value without widening

Make `wrap` preserve the caller's value type in the returned array.

## Constraints

- Keep one implementation.
- Preserve literal input types.
- Do not use `any` or assertions.
