# TS-GEN-002 — Build a property accessor

Implement `getProperty` so the key must exist on the object and the returned type corresponds exactly to that key.

## Constraints

- Do not use `any` or assertions.
- Reject keys that are absent from the object.
- Preserve different result types for different keys.
