# TS-GEN-080 — Tap a generic value

Run a side effect without losing the exact type of the tapped value.

## Constraints

- The callback must receive the value type.
- Return the original value type.
- Do not use assertions.
