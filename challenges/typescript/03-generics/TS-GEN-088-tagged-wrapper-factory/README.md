# TS-GEN-088 — Create a tagged wrapper

Wrap a value with a tag while preserving both inferred types.

## Constraints

- Preserve the value type.
- Preserve the tag literal type.
- Return a new wrapper object.
