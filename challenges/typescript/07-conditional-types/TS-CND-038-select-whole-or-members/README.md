# TS-CND-038 — Select whole-union or member mode

Implement `SelectMode` to select whole-union or member mode, while preserving the generic relationships in the contract.

## Constraints

- Model the selected whole-union or member-wise mode with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
