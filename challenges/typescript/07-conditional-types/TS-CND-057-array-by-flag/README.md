# TS-CND-057 — Wrap in array by flag

Implement `ArrayIf` to wrap in array by flag, while preserving the generic relationships in the contract.

## Constraints

- Model the multiple-value flag with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
