# TS-CND-021 — Keep property-key members

Implement `PropertyKeys` to keep property-key members, while preserving the generic relationships in the contract.

## Constraints

- Model PropertyKey with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
