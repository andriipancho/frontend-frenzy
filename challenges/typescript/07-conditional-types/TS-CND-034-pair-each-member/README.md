# TS-CND-034 — Pair each member with context

Implement `PairEach` to pair each member with context, while preserving the generic relationships in the contract.

## Constraints

- Model each union member independently with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
