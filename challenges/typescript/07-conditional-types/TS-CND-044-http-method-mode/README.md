# TS-CND-044 — Classify HTTP method behavior

Implement `MethodMode` to classify HTTP method behavior, while preserving the generic relationships in the contract.

## Constraints

- Model the method groups in the contract with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
