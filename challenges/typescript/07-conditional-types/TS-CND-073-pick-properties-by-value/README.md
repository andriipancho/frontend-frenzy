# TS-CND-073 — Pick properties by value type

Implement `PickByValue` to pick properties by value type, while preserving the generic relationships in the contract.

## Constraints

- Model each property's assignability to the requested value type with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
