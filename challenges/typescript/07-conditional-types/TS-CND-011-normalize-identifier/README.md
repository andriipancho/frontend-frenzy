# TS-CND-011 — Normalize identifier types

Implement `NormalizedId` to normalize identifier types, while preserving the generic relationships in the contract.

## Constraints

- Model string and number identifier inputs with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
