# TS-CND-027 — Keep members containing a key

Implement `WithKey` to keep members containing a key, while preserving the generic relationships in the contract.

## Constraints

- Model presence of the supplied key on each member with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
