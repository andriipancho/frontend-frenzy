# TS-CND-056 — Add null by flag

Implement `NullableIf` to add null by flag, while preserving the generic relationships in the contract.

## Constraints

- Model the enabled flag with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
