# TS-CND-029 — Check for a matching union member

Implement `SomeExtend` to check for a matching union member, while preserving the generic relationships in the contract.

## Constraints

- Model at least one distributively matching member with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
