# TS-CND-099 — Design validation outcomes

Implement `ValidationOutcome` to design validation outcomes, while preserving the generic relationships in the contract.

## Constraints

- Model the validation result flag with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
