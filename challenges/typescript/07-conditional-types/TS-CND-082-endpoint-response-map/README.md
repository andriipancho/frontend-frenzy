# TS-CND-082 — Map endpoint response values

Implement `EndpointResponses` to map endpoint response values, while preserving the generic relationships in the contract.

## Constraints

- Model whether each endpoint contains a response contract with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
