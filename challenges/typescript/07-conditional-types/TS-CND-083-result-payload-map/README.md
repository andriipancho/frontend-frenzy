# TS-CND-083 — Map successful result payloads

Implement `SuccessPayloads` to map successful result payloads, while preserving the generic relationships in the contract.

## Constraints

- Model whether each result is a successful data-bearing shape with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
