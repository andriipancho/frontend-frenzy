# TS-CND-026 — Select exclusive union members

Implement `ExclusiveMembers` to select exclusive union members, while preserving the generic relationships in the contract.

## Constraints

- Model membership in exactly one of the two unions with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
