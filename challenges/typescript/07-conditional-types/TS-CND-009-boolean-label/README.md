# TS-CND-009 — Label boolean literals

Implement `BooleanLabel` to label boolean literals, while preserving the generic relationships in the contract.

## Constraints

- Model the true boolean literal with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
