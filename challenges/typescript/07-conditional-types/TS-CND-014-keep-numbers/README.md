# TS-CND-014 — Keep number members

Implement `KeepNumbers` to keep number members, while preserving the generic relationships in the contract.

## Constraints

- Model number with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
