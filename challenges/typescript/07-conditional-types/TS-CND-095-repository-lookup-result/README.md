# TS-CND-095 — Design repository lookup results

Implement `RepositoryLookup` to design repository lookup results, while preserving the generic relationships in the contract.

## Constraints

- Model the strict lookup mode with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
