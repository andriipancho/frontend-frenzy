# TS-CND-089 — Model ownership access decisions

Implement `AccessDecision` to model ownership access decisions, while preserving the generic relationships in the contract.

## Constraints

- Model role privilege before resource ownership with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
