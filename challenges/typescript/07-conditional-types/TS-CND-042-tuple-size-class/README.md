# TS-CND-042 — Classify tuple size

Implement `TupleSize` to classify tuple size, while preserving the generic relationships in the contract.

## Constraints

- Model empty and single-element tuple shapes before the general case with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
