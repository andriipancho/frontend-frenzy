# TS-CND-023 — Filter by assignability

Implement `FilterAssignable` to filter by assignability, while preserving the generic relationships in the contract.

## Constraints

- Model the supplied constraint with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
