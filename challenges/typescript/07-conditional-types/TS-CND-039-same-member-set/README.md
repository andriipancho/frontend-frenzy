# TS-CND-039 — Compare union member sets

Implement `SameMembers` to compare union member sets, while preserving the generic relationships in the contract.

## Constraints

- Model bidirectional whole-union assignability with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
