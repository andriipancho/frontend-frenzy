# TS-CND-069 — Select parameters or fallback

Implement `ParametersOr` to select parameters or fallback, while preserving the generic relationships in the contract.

## Constraints

- Model a callable signature with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
