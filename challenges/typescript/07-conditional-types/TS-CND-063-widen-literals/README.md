# TS-CND-063 — Widen primitive literals

Implement `WidenLiteral` to widen primitive literals, while preserving the generic relationships in the contract.

## Constraints

- Model primitive literal categories with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
