# TS-CND-007 — Keep callable types

Implement `CallableOnly` to keep callable types, while preserving the generic relationships in the contract.

## Constraints

- Model a callable signature with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
