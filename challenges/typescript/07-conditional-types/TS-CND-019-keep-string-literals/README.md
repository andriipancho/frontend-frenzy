# TS-CND-019 — Keep only string literals

Implement `StringLiterals` to keep only string literals, while preserving the generic relationships in the contract.

## Constraints

- Model literal strings but not the broad string type with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
