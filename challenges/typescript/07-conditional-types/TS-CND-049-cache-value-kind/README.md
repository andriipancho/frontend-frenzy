# TS-CND-049 — Classify cache values

Implement `CacheValueKind` to classify cache values, while preserving the generic relationships in the contract.

## Constraints

- Model nullish, asynchronous, structured, and scalar values in order with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
