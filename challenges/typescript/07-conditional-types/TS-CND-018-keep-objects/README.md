# TS-CND-018 — Keep object members

Implement `KeepObjects` to keep object members, while preserving the generic relationships in the contract.

## Constraints

- Model object with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
