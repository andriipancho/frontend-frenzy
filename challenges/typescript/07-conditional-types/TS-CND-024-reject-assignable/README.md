# TS-CND-024 — Reject assignable members

Implement `RejectAssignable` to reject assignable members, while preserving the generic relationships in the contract.

## Constraints

- Model the supplied rejected constraint with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
