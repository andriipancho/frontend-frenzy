# TS-CND-028 — Check every union member

Implement `AllExtend` to check every union member, while preserving the generic relationships in the contract.

## Constraints

- Model assignability of the union as a whole with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
