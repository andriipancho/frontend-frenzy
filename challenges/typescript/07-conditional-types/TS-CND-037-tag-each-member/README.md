# TS-CND-037 — Tag every union member

Implement `TaggedMember` to tag every union member, while preserving the generic relationships in the contract.

## Constraints

- Model each union member independently with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
