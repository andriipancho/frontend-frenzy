# TS-CND-067 — Select elements or fallback

Implement `ElementsOr` to select elements or fallback, while preserving the generic relationships in the contract.

## Constraints

- Model readonly array inputs with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
