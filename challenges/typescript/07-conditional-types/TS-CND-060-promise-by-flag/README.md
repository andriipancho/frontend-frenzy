# TS-CND-060 — Wrap in promise by flag

Implement `PromiseIf` to wrap in promise by flag, while preserving the generic relationships in the contract.

## Constraints

- Model the asynchronous flag with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
