# TS-CND-002 — Detect string types

Implement `IsString` to detect string types, while preserving the generic relationships in the contract.

## Constraints

- Model the string type with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
