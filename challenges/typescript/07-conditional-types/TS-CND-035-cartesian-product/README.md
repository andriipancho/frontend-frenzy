# TS-CND-035 — Build a union cartesian product

Implement `CrossProduct` to build a union cartesian product, while preserving the generic relationships in the contract.

## Constraints

- Model each member of both unions with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
