# TS-CND-015 — Keep boolean members

Implement `KeepBooleans` to keep boolean members, while preserving the generic relationships in the contract.

## Constraints

- Model boolean with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
