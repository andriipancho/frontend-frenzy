# TS-CND-070 — Select instance or fallback

Implement `InstanceOr` to select instance or fallback, while preserving the generic relationships in the contract.

## Constraints

- Model an abstract constructor signature with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
