# TS-CND-072 — Reject keys by value type

Implement `KeysNotMatching` to reject keys by value type, while preserving the generic relationships in the contract.

## Constraints

- Model each property's rejection of the requested value type with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
