# TS-CND-008 — Fall back for non-objects

Implement `ObjectOr` to fall back for non-objects, while preserving the generic relationships in the contract.

## Constraints

- Model the object constraint with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
