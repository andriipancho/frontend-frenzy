# TS-CND-047 — Classify JSON-like values

Implement `JsonCategory` to classify JSON-like values, while preserving the generic relationships in the contract.

## Constraints

- Model the ordered JSON-like categories with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
