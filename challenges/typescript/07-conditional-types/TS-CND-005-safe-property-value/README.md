# TS-CND-005 — Select a safe property value

Implement `PropertyValue` to select a safe property value, while preserving the generic relationships in the contract.

## Constraints

- Model membership in the object's keys with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
