# TS-CND-097 — Select delivery channel payloads

Implement `DeliveryPayload` to select delivery channel payloads, while preserving the generic relationships in the contract.

## Constraints

- Model each message member's delivery channel with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
