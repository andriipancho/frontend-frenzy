# TS-CND-022 — Keep array members

Implement `ArrayMembers` to keep array members, while preserving the generic relationships in the contract.

## Constraints

- Model readonly array types with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
