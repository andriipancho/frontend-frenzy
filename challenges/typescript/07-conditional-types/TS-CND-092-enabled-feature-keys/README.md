# TS-CND-092 — Select enabled feature keys

Implement `EnabledFeatures` to select enabled feature keys, while preserving the generic relationships in the contract.

## Constraints

- Model the literal true state of each feature with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
