# TS-CND-006 — Select element or original type

Implement `ElementOrSelf` to select element or original type, while preserving the generic relationships in the contract.

## Constraints

- Model readonly array types with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
