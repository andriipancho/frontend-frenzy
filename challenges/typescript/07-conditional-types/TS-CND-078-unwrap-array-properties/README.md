# TS-CND-078 — Unwrap array properties

Implement `UnwrapArrayProperties` to unwrap array properties, while preserving the generic relationships in the contract.

## Constraints

- Model whether each property is an array with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
