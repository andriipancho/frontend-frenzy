# TS-CND-016 — Remove string members

Implement `WithoutStrings` to remove string members, while preserving the generic relationships in the contract.

## Constraints

- Model string with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
