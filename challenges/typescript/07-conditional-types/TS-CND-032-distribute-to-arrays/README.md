# TS-CND-032 — Create an array per union member

Implement `ArrayPerMember` to create an array per union member, while preserving the generic relationships in the contract.

## Constraints

- Model each member independently with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
