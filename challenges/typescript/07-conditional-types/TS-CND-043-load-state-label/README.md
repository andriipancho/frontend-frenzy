# TS-CND-043 — Classify load states

Implement `LoadStateLabel` to classify load states, while preserving the generic relationships in the contract.

## Constraints

- Model the discriminant values in priority order with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
