# TS-CND-065 — Reject empty tuples

Implement `NonEmptyTuple` to reject empty tuples, while preserving the generic relationships in the contract.

## Constraints

- Model the empty tuple shape with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
