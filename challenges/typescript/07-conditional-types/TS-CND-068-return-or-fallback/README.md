# TS-CND-068 — Select function return or fallback

Implement `ReturnOr` to select function return or fallback, while preserving the generic relationships in the contract.

## Constraints

- Model a callable signature with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
