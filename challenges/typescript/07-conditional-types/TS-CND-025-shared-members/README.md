# TS-CND-025 — Select shared union members

Implement `SharedMembers` to select shared union members, while preserving the generic relationships in the contract.

## Constraints

- Model membership in the right-hand union with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
