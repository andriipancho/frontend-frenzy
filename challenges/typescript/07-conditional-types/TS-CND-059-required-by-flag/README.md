# TS-CND-059 — Require properties by flag

Implement `RequiredIf` to require properties by flag, while preserving the generic relationships in the contract.

## Constraints

- Model the completeness flag with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
