# TS-CND-061 — Model a property lookup result

Implement `PropertyLookup` to model a property lookup result, while preserving the generic relationships in the contract.

## Constraints

- Model whether the requested key exists with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
