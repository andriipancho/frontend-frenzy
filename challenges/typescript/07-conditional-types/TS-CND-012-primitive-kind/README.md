# TS-CND-012 — Classify primitive kinds

Implement `PrimitiveKind` to classify primitive kinds, while preserving the generic relationships in the contract.

## Constraints

- Model each supported primitive category with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
