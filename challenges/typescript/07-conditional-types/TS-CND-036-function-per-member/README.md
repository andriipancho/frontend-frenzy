# TS-CND-036 — Create a function per member

Implement `FunctionPerMember` to create a function per member, while preserving the generic relationships in the contract.

## Constraints

- Model each union member independently with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
