# TS-CND-062 — Accept compatible updates

Implement `CompatibleUpdate` to accept compatible updates, while preserving the generic relationships in the contract.

## Constraints

- Model whole-update assignability to the current contract with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
