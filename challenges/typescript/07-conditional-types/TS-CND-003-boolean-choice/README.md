# TS-CND-003 — Choose by boolean flag

Implement `If` to choose by boolean flag, while preserving the generic relationships in the contract.

## Constraints

- Model the true literal with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
