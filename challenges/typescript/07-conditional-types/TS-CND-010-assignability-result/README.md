# TS-CND-010 — Report assignability

Implement `IsAssignable` to report assignability, while preserving the generic relationships in the contract.

## Constraints

- Model assignability from the first type to the second with conditional branch selection.
- Preserve exact literal, property, and generic relationships in the resulting type.
- Do not use any.
