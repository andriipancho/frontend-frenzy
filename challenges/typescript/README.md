# TypeScript challenge bank

The TypeScript chapter progresses from precise core types through narrowing, generic relationships, type transformation, recursive modeling, mixed recognition, and production API design. Topic numbers define curriculum order; stable challenge IDs define identity.

Starter files are intentionally unsolved and excluded from the root build. Use `frenzy check` to compile only the active challenge and its tests.
