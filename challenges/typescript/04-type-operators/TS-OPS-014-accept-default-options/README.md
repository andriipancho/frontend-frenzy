# TS-OPS-014 — Accept the default option shape

Type `normalizeOptions` from `defaultOptions` instead of duplicating the object shape.

## Constraints

- Keep the default object unchanged.
- Accept objects matching its inferred shape.
- Do not declare a second structural type.
