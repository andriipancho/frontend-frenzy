# TS-OPS-030 — Pluck selected values

Type `pluck` so its result contains values belonging to the supplied key union.

## Constraints

- Accept multiple valid keys.
- Reject keys absent from the object.
- Return an array of the selected property-value union.
