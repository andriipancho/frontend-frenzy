# TS-OPS-051 — Select related fields

Return values for selected object keys while preserving their combined value type.

## Constraints

- Accept only keys from the supplied object.
- Allow a readonly key list.
- Return the union of values for the selected keys.
