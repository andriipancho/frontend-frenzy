# TS-OPS-013 — Reuse a formatter signature

Expose the exact function type of the existing formatter.

## Constraints

- Keep `formatDate` as the source of truth.
- Do not restate its parameters.
- Preserve its return type.
