# 04 Type operators

`keyof`, `typeof`, indexed access, `as const`, `satisfies`, and key/value relationships.

Status: 75 / 75 challenges.

| Range | Focus |
| --- | --- |
| `TS-OPS-001`–`010` | `keyof`, constrained keys, and object key relationships |
| `TS-OPS-011`–`020` | Deriving types from runtime values with `typeof` |
| `TS-OPS-021`–`030` | Indexed access across objects, arrays, tuples, and unions |
| `TS-OPS-031`–`040` | Const assertions and literal extraction |
| `TS-OPS-041`–`050` | Shape validation with `satisfies` |
| `TS-OPS-051`–`060` | Key/value relationships in reusable operations |
| `TS-OPS-061`–`068` | Derived application contracts and nested selection |
| `TS-OPS-069`–`075` | Production API relationships from a single source of truth |
