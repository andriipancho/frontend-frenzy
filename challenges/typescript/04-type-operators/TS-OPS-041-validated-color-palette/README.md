# TS-OPS-041 — Validate a color palette

Validate hexadecimal color values while preserving exact keys and color literals.

## Constraints

- Every value must begin with `#`.
- Keep exact color literals available.
- Make the palette deeply readonly.
