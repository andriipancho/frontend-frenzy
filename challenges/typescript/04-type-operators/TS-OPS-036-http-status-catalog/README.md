# TS-OPS-036 — Model an HTTP status catalog

Derive both status names and exact numeric codes from one readonly lookup.

## Constraints

- Keep one runtime status object.
- Do not repeat keys or values in aliases.
- Preserve numeric literals.
