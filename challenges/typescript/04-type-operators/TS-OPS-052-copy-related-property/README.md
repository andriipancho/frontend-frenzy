# TS-OPS-052 — Copy a related property

Copy one selected property between objects without losing the key-to-value relationship.

## Constraints

- Use the same selected key on source and target.
- Reject keys absent from the object type.
- Do not use `any` or assertions.
