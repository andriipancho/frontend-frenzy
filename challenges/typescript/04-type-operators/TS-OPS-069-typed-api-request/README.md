# TS-OPS-069 — Type API requests by route

Couple each API route with the request body declared for that route.

## Constraints

- Accept only declared routes.
- Require the body matching the selected route.
- Return the selected route's response type.
