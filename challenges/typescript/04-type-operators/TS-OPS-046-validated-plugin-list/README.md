# TS-OPS-046 — Validate a plugin list

Validate plugin objects while retaining an exact union of their names.

## Constraints

- Each plugin needs a name and setup function.
- Keep plugin names as string literals.
- Keep the list readonly.
