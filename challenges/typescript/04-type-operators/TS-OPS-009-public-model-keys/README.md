# TS-OPS-009 — Expose public model keys

Derive the public property names available on `SessionModel` instances.

## Constraints

- Do not repeat method or property names.
- Exclude private implementation state naturally.
- Keep the class definition unchanged.
