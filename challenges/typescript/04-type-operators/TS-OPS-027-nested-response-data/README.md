# TS-OPS-027 — Select nested response data

Derive the payload type nested inside an API response.

## Constraints

- Keep the response contract unchanged.
- Do not duplicate the payload shape.
- Traverse through both property levels.
