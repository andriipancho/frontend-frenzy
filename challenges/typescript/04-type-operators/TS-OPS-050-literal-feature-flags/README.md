# TS-OPS-050 — Preserve validated feature flags

Validate a boolean feature map while retaining exact `true` and `false` values.

## Constraints

- Every declared value must be boolean.
- Keep the concrete flag names.
- Make exact boolean literals available to consumers.
