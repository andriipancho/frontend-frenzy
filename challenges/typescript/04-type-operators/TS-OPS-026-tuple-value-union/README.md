# TS-OPS-026 — Collect tuple value types

Derive a union containing every value type in a metrics tuple.

## Constraints

- Keep the tuple definition unchanged.
- Include all tuple positions.
- Do not manually build the union.
