# TS-OPS-055 — Filter with a property predicate

Type a field predicate from the exact property selected for filtering.

## Constraints

- Infer the predicate parameter type.
- Reject keys missing from collection items.
- Return items rather than property values.
