# TS-OPS-029 — Create a property value helper

Implement a reusable type helper that selects a property type by a valid key.

## Constraints

- Accept any object-like type parameter.
- Restrict the key to that type's keys.
- Return the exact selected property type.
