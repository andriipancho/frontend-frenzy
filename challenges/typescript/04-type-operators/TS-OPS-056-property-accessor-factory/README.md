# TS-OPS-056 — Create a property accessor

Create a reusable getter for one property name that preserves each object's value type.

## Constraints

- Capture the key when creating the accessor.
- Require later objects to contain that key.
- Return the concrete property type for each object.
