# TS-OPS-020 — Derive a validator map

Expose the complete type of the runtime validator registry.

## Constraints

- Keep validator implementations intact.
- Do not redeclare their signatures.
- Preserve each named function separately.
