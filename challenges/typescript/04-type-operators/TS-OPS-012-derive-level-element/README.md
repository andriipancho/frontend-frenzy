# TS-OPS-012 — Derive an array element type

Derive the element type of `levels` without declaring the value union separately.

## Constraints

- Keep `levels` as the source of truth.
- Preserve its literal elements.
- Do not repeat the level names.
