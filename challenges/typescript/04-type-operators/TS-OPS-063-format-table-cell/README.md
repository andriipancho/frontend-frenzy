# TS-OPS-063 — Format a table cell

Type a cell formatter from the exact row field selected at the call site.

## Constraints

- Restrict column names to row keys.
- Infer the formatter input from the column.
- Return a string label.
