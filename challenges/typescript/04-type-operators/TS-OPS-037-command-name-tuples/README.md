# TS-OPS-037 — Derive command names

Derive valid command names from the first position of readonly command tuples.

## Constraints

- Keep each command paired with its permission.
- Do not repeat command names.
- Exclude permission values from the result.
