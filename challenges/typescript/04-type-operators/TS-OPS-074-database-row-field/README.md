# TS-OPS-074 — Read a database row field

Relate a table name, its row type, and the selected field in one query helper.

## Constraints

- Reject unknown tables.
- Restrict fields to the selected table's row.
- Return the exact field type.
