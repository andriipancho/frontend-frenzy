# TS-OPS-023 — Select an audit entry

Derive the element type stored in an audit log array.

## Constraints

- Keep the array property as the source of truth.
- Do not duplicate the entry object shape.
- Select one array element type.
