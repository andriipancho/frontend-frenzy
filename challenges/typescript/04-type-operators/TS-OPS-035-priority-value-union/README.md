# TS-OPS-035 — Derive priority values

Create a numeric literal union from an enum-like runtime object.

## Constraints

- Keep the object as the runtime lookup.
- Do not repeat numeric values.
- Exclude arbitrary numbers.
