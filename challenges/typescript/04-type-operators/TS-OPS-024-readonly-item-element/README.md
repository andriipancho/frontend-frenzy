# TS-OPS-024 — Select a readonly list element

Derive an item type from a readonly array property.

## Constraints

- Keep the collection readonly.
- Do not restate the item shape.
- Return the element rather than the array type.
