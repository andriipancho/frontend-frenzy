# TS-OPS-054 — Filter by a property value

Type a collection filter so the comparison value matches the selected field.

## Constraints

- Keep the equality-based runtime filter.
- Restrict the key to collection item keys.
- Return the original item type.
