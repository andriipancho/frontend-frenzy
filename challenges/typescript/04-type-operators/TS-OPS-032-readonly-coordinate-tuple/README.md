# TS-OPS-032 — Preserve a coordinate tuple

Infer the coordinate as an exact readonly tuple rather than a mutable number array.

## Constraints

- Keep the two runtime coordinates.
- Preserve both numeric literals.
- Do not write an explicit tuple annotation.
