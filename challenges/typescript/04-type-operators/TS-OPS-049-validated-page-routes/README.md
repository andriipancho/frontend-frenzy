# TS-OPS-049 — Validate page routes

Validate route strings with a leading slash while preserving exact route values.

## Constraints

- Every route must begin with `/`.
- Keep exact route names and paths.
- Make the route table readonly.
