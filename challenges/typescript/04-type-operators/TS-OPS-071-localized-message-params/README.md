# TS-OPS-071 — Type localized message parameters

Require interpolation parameters that match each localized message key.

## Constraints

- Keep one translation function.
- Reject parameters for another message.
- Allow the parameterless message only with its empty contract.
