# TS-OPS-061 — Select an app state slice

Return the exact slice type associated with a key of `AppState`.

## Constraints

- Keep `AppState` as the state contract.
- Reject unknown slice names.
- Preserve the selected slice type.
