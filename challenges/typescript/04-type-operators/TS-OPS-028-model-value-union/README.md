# TS-OPS-028 — Collect model value types

Derive a union of every property value type declared by `Model`.

## Constraints

- Keep `Model` as the source of truth.
- Include values from all properties.
- Do not write the value union manually.
