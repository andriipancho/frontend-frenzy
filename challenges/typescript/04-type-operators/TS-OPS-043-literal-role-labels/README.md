# TS-OPS-043 — Preserve validated role labels

Validate a complete role-label map while preserving every exact label string.

## Constraints

- Include every declared role.
- Keep values readonly literal strings.
- Do not annotate the object directly.
