# TS-OPS-017 — Derive message values

Produce the literal message-value union from the runtime catalog.

## Constraints

- Keep exact message text literals.
- Do not repeat values in a type alias.
- Include values from every key.
