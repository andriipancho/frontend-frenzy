# TS-OPS-073 — Track typed analytics events

Couple analytics event names to their exact payload contracts.

## Constraints

- Keep one tracking implementation.
- Reject event and payload mismatches.
- Allow every declared event.
