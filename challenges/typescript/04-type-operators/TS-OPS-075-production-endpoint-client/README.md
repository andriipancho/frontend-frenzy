# TS-OPS-075 — Design a derived endpoint client

Derive endpoint names, request inputs, and response outputs from one production endpoint contract.

## Constraints

- Keep endpoint contracts as the only source of truth.
- Reject mismatched endpoint requests.
- Preserve each endpoint's response type.
