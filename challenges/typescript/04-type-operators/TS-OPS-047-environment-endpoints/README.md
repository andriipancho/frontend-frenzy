# TS-OPS-047 — Validate environment endpoints

Validate every environment endpoint and retain each exact URL literal.

## Constraints

- Cover development, staging, and production.
- Require HTTPS-shaped endpoint strings.
- Keep all endpoint values readonly.
