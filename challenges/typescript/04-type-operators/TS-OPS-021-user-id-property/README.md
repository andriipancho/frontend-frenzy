# TS-OPS-021 — Select a user ID type

Derive the `id` property type from `User` using indexed access.

## Constraints

- Keep `User` unchanged.
- Do not repeat the primitive type.
- Select only the `id` property.
