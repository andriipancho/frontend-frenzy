# TS-UTL-022 — Map numeric response codes

Create a complete lookup from supported numeric response codes to response metadata.

## Constraints

- Keep keys numeric.
- Require metadata for every code.
- Use one shared metadata shape.
