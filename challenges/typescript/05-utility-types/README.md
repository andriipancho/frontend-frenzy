# 05 Utility types

Using and reimplementing the standard utility types that appear in production TypeScript.

Status: 75 / 75 challenges.

| Range | Focus |
| --- | --- |
| `TS-UTL-001`–`010` | Optionality and immutability with `Partial`, `Required`, and `Readonly` |
| `TS-UTL-011`–`020` | Selecting and removing fields with `Pick` and `Omit` |
| `TS-UTL-021`–`030` | Complete and sparse dictionaries with `Record` |
| `TS-UTL-031`–`040` | Union filtering with `Exclude`, `Extract`, and `NonNullable` |
| `TS-UTL-041`–`050` | Function and constructor argument extraction |
| `TS-UTL-051`–`060` | Return, instance, and resolved async types |
| `TS-UTL-061`–`071` | Reimplementing foundational utility types |
| `TS-UTL-072`–`075` | Production DTO, command, and factory contracts |
