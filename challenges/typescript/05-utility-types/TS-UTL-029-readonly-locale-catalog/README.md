# TS-UTL-029 — Protect a locale catalog

Model a complete translation catalog whose entries cannot be reassigned.

## Constraints

- Require every supported locale.
- Keep message values strings.
- Make top-level locale entries readonly.
