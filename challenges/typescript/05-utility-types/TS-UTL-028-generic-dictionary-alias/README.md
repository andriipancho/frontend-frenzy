# TS-UTL-028 — Create a generic dictionary alias

Implement a reusable dictionary type for a valid property-key union and one value type.

## Constraints

- Support string, number, and symbol keys.
- Keep both type parameters.
- Require every key in the supplied union.
