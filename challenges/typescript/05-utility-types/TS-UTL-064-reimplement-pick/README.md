# TS-UTL-064 — Reimplement Pick

Implement `MyPick` so it selects a valid subset of object properties.

## Constraints

- Restrict selected keys to `keyof T`.
- Preserve property modifiers and value types.
- Do not use the built-in `Pick` utility.
