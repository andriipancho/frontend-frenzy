# TS-UTL-067 — Reimplement Exclude

Implement `MyExclude` so matching members are removed from a union.

## Constraints

- Distribute across union members.
- Remove members assignable to U.
- Do not use the built-in `Exclude` utility.
