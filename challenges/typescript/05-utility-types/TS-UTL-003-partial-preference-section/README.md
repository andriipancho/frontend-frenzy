# TS-UTL-003 — Model partial display preferences

Make only the nested display preference fields optional.

## Constraints

- Keep the outer preferences model unchanged.
- Do not make notification fields part of this patch.
- Do not duplicate nested property types.
