# TS-UTL-068 — Reimplement Extract

Implement `MyExtract` so only matching union members remain.

## Constraints

- Distribute across union members.
- Keep members assignable to U.
- Do not use the built-in `Extract` utility.
