# TS-UTL-066 — Reimplement Record

Implement `MyRecord` so every key in a property-key union maps to one value type.

## Constraints

- Support string, number, and symbol keys.
- Require every supplied key.
- Do not use the built-in `Record` utility.
