# TS-UTL-074 — Derive command call contracts

Derive each command's argument tuple and return type from one command function map.

## Constraints

- Accept only declared command keys.
- Preserve arguments for the selected command.
- Preserve synchronous or asynchronous return types.
