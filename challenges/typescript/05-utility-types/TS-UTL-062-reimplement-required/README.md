# TS-UTL-062 — Reimplement Required

Implement `MyRequired` so optional properties become required.

## Constraints

- Preserve keys and value types.
- Remove optional modifiers from every property.
- Do not use the built-in `Required` utility.
