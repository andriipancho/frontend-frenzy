# TS-UTL-036 — Extract user events

Derive event names that follow the `user:` namespace pattern.

## Constraints

- Keep only user-prefixed events.
- Do not list retained events separately.
- Use the existing event-name union.
