# TS-UTL-023 — Map features to configuration

Require configuration for each known feature name.

## Constraints

- Cover every feature.
- Use the same configuration contract for each key.
- Reject arbitrary string keys in the type.
