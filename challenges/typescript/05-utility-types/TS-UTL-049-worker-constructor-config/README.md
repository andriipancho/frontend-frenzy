# TS-UTL-049 — Select worker constructor config

Derive the configuration object from the first worker constructor argument.

## Constraints

- Keep the worker class as the source of truth.
- Select only the configuration argument.
- Do not duplicate the object shape.
