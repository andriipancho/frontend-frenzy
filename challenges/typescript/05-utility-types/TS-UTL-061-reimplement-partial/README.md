# TS-UTL-061 — Reimplement Partial

Implement `MyPartial` so every property of an object type becomes optional.

## Constraints

- Preserve property names and value types.
- Work for arbitrary object types.
- Do not use the built-in `Partial` utility.
