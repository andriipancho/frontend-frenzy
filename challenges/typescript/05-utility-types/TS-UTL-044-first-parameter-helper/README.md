# TS-UTL-044 — Create a first-parameter helper

Implement a reusable type that selects the first argument from a function type.

## Constraints

- Accept callable types only.
- Preserve the exact first parameter type.
- Use standard utilities rather than conditional inference.
