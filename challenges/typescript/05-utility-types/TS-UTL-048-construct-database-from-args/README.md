# TS-UTL-048 — Forward database constructor arguments

Type a factory rest parameter directly from the database constructor.

## Constraints

- Keep one forwarding implementation.
- Preserve optional constructor arguments.
- Return a `Database` instance.
