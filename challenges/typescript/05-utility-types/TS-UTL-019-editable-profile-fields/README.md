# TS-UTL-019 — Model editable profile fields

Create an update input that excludes identity and permits any subset of editable fields.

## Constraints

- Exclude the immutable id.
- Make all remaining properties optional.
- Preserve original field value types.
