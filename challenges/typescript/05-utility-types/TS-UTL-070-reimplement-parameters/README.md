# TS-UTL-070 — Reimplement Parameters

Implement `MyParameters` so it extracts the argument tuple from a function type.

## Constraints

- Accept callable types only.
- Preserve labels and optional arguments.
- Do not use the built-in `Parameters` utility.
