# TS-UTL-065 — Reimplement Omit

Implement `MyOmit` so selected property names are removed from an object type.

## Constraints

- Keep every non-excluded property.
- Accept property keys that may not exist on T.
- Do not use the built-in `Omit` utility.
