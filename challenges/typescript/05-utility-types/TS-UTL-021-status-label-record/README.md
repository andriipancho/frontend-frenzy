# TS-UTL-021 — Create a status label record

Require a string label for every declared order status.

## Constraints

- Cover every status key.
- Reject unknown status keys.
- Keep all values strings.
