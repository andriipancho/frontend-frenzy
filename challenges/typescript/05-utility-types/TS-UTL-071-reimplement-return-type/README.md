# TS-UTL-071 — Reimplement ReturnType

Implement `MyReturnType` so it extracts the result type of a function.

## Constraints

- Accept callable types only.
- Preserve unions and Promise wrappers.
- Do not use the built-in `ReturnType` utility.
