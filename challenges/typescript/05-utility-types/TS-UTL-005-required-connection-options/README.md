# TS-UTL-005 — Require connection options

Derive a resolved connection type where every option is present.

## Constraints

- Keep optional input modeling in `ConnectionOptions`.
- Do not duplicate the properties.
- Remove optionality from all fields.
