# TS-UTL-041 — Derive logger parameters

Expose the complete argument tuple of the existing logger function.

## Constraints

- Keep parameter order and optionality.
- Do not restate argument types.
- Derive from the function value.
