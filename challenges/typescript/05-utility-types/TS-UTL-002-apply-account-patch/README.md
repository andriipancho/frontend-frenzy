# TS-UTL-002 — Apply an account patch

Accept partial account updates while returning a complete account.

## Constraints

- Require the original complete account.
- Allow any subset of account fields in the patch.
- Return the full `Account` contract.
