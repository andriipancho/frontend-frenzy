# TS-UTL-042 — Derive callback parameters

Create a reusable alias that extracts the argument tuple from a callback type.

## Constraints

- Accept function types only.
- Preserve tuple labels and optional parameters.
- Do not manually infer individual arguments.
