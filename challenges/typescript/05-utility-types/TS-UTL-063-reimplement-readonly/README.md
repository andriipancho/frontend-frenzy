# TS-UTL-063 — Reimplement Readonly

Implement `MyReadonly` so every property becomes immutable.

## Constraints

- Preserve keys, optionality, and value types.
- Add readonly to every property.
- Do not use the built-in `Readonly` utility.
