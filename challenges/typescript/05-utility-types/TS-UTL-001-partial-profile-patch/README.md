# TS-UTL-001 — Model a partial profile patch

Define a patch type where every `Profile` field can be supplied independently.

## Constraints

- Keep `Profile` as the source of truth.
- Do not repeat its property declarations.
- Allow an empty patch.
