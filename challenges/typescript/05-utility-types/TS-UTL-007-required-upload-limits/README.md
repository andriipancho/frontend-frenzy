# TS-UTL-007 — Require upload limits

Derive a fully specified upload limit type from a nested optional section.

## Constraints

- Keep `UploadPolicy` unchanged.
- Select only the limits section.
- Require both limit fields.
