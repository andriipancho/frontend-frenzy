# TS-UTL-030 — Model sparse metrics

Allow a metrics snapshot to contain any subset of known numeric metrics.

## Constraints

- Reject unknown metric names.
- Keep every supplied value numeric.
- Do not require every metric in each snapshot.
