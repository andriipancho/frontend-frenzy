# TS-UTL-014 — Select shipping label fields

Derive the address fields printed on a shipping label from a nested model.

## Constraints

- Select name, city, postal code, and country.
- Exclude internal delivery instructions.
- Do not duplicate the nested property types.
