# TS-UTL-017 — Sanitize a session result

Return a session object without its transport token.

## Constraints

- Keep the runtime destructuring behavior.
- Return every remaining session property.
- Derive the return contract with a utility type.
