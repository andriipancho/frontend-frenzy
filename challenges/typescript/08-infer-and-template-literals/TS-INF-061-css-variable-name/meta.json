{
  "id": "TS-INF-061",
  "title": "Generate CSS variable names",
  "domain": "typescript",
  "topic": "infer-and-template-literals",
  "difficulty": "easy",
  "topics": [
    "template-literal-types",
    "css",
    "generated-names"
  ],
  "estimatedMinutes": 4,
  "prerequisites": [
    "conditional-types"
  ],
  "hints": [
    "Identify the fixed and generic string segments that represent the custom-property prefix and supplied name.",
    "Use a template literal type to compose the output from those segments.",
    "Apply intrinsic string transforms or mapped key remapping only where the contract requires them."
  ],
  "validation": {
    "type": "typescript"
  },
  "constraints": {
    "forbid": [
      "any"
    ]
  }
}
