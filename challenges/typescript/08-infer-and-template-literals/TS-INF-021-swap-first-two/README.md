# TS-INF-021 — Swap the first two tuple elements

Implement `SwapFirstTwo` to swap the first two tuple elements, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from the first two positions and remaining tuple segment.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
