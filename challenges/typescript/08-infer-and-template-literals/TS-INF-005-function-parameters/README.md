# TS-INF-005 — Infer parameter tuples

Implement `ParameterTuple` to infer parameter tuples, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a function's complete parameter list.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
