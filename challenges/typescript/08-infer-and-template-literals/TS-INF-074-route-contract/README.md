# TS-INF-074 — Parse route contracts

Implement `RouteContract` to parse route contracts, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a constrained method and path in a route contract.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
