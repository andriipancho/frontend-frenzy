# TS-INF-003 — Infer function return types

Implement `FunctionResult` to infer function return types, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a function's return position.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
