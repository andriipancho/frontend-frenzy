# TS-INF-017 — Infer both tuple ends

Implement `TupleEnds` to infer both tuple ends, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from the first and final positions of a tuple.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
