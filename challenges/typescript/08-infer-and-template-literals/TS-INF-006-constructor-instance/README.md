# TS-INF-006 — Infer constructor instances

Implement `Constructed` to infer constructor instances, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a constructor's instance position.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
