# TS-INF-001 — Infer array element types

Implement `ElementType` to infer array element types, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from an array's element position.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
