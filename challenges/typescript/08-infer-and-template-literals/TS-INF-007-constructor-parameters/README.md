# TS-INF-007 — Infer constructor parameter tuples

Implement `ConstructorParametersOf` to infer constructor parameter tuples, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a constructor's complete parameter list.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
