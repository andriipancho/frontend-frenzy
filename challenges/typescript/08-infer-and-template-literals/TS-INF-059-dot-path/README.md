# TS-INF-059 — Build dot-separated paths

Implement `DotPath` to build dot-separated paths, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from parent and child path segments.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
