# TS-INF-016 — Infer tuple initial segments

Implement `TupleInit` to infer tuple initial segments, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from the tuple positions before the final element.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
