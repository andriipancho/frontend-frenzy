# TS-INF-013 — Infer tuple first elements

Implement `TupleFirst` to infer tuple first elements, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from the first position of a tuple.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
