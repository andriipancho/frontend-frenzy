# TS-INF-014 — Infer tuple last elements

Implement `TupleLast` to infer tuple last elements, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from the final position of a tuple.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
