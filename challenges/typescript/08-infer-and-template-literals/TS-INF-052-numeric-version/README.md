# TS-INF-052 — Infer numeric version literals

Implement `VersionNumber` to infer numeric version literals, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a numeric literal following the version prefix.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
