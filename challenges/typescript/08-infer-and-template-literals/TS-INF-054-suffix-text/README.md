# TS-INF-054 — Build suffixed string types

Implement `SuffixedText` to build suffixed string types, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from both supplied string literal positions.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
