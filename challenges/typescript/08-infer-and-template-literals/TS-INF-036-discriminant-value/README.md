# TS-INF-036 — Infer discriminant values

Implement `DiscriminantOf` to infer discriminant values, while preserving the inferred relationships in the contract.

## Constraints

- Derive the output from a property-key discriminant position.
- Preserve exact literal, tuple, and generic relationships in the resulting type.
- Do not use any.
